# SkyCooker HA - Тестирование подключения

## Быстрое тестирование

### 1. Сканирование устройств

Запустите тестовый скрипт для поиска вашей мультиварки:

```bash
python test_connection.py
```

Скрипт покажет все доступные Bluetooth устройства и выделит те, что похожи на Redmond устройства (начинающиеся с "RMC-" или "RK-").

### 2. Подключение к мультиварке

Для тестирования подключения вам нужно:

1. **Найти MAC-адрес вашей мультиварки** из вывода сканирования
2. **Включить режим пары** на мультиварке (обычно удерживанием кнопки питания 3-5 секунд)
3. **Обновить параметры в скрипте**:

```python
# В файле test_connection.py замените:
mac_address = "XX:XX:XX:XX:XX:XX"  # На реальный MAC-адрес
key = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07]  # Стандартный ключ
model = "RMC-M40S"  # Ваша модель
```

### 3. Запуск теста

```bash
python test_connection.py
```

### 4. Интерпретация результатов

**Успешное подключение:**
```
✓ Подключение успешно!
  Версия прошивки: (1, 0)
  Текущая температура: 25
  Текущий режим: None
  Целевая температура: 25
  Целевой режим: None
  Успешность подключений: 100%
```

**Ошибки подключения:**

- **"Auth failed"** - Неверный ключ аутентификации или мультиварка не в режиме пары
- **"not connected"** - Не удалось установить Bluetooth соединение
- **"Receive timeout"** - Проблемы с Bluetooth или расстояние до устройства

## Поиск MAC-адреса

Если вы не видите вашу мультиварку в списке:

1. **Проверьте Bluetooth** - Убедитесь, что Bluetooth включен на вашем сервере
2. **Включите режим пары** на мультиварке
3. **Проверьте расстояние** - Мультиварка должна быть в пределах 10 метров
4. **Попробуйте перезагрузить Bluetooth**:

```bash
sudo systemctl restart bluetooth
```

## Типичные проблемы

### 1. "BleakOutOfConnectionSlotsError"

**Решение:** Используйте постоянное подключение (persistent=True) как в нашем обновленном коде.

### 2. "Auth failed"

**Решение:**
- Убедитесь, что мультиварка в режиме пары (индикатор мигает)
- Попробуйте стандартный ключ: `[0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07]`
- Некоторые устройства используют другие ключи, попробуйте поискать в документации

### 3. Нет устройств в списке

**Решение:**
- Проверьте, что мультиварка включена и в режиме пары
- Проверьте Bluetooth адаптер
- Попробуйте запустить сканирование с правами root: `sudo python test_connection.py`

## Тестирование функций

После успешного подключения вы можете протестировать основные функции:

```python
# Вручную в скрипте после успешного подключения:

# Включить мультиварку
await cooker.turn_on()

# Установить режим "Суп"
await cooker.set_target_mode("Soup")

# Установить температуру 80°C
await cooker.set_target_temp(80)

# Установить таймер на 30 минут
await cooker.set_boil_time(30)

# Выключить мультиварку
await cooker.turn_off()
```

## Логирование

Для детального анализа проблем включите подробное логирование:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

Это покажет все Bluetooth команды и ответы.

## Следующие шаги

После успешного тестирования:

1. **Установите интеграцию** в HomeAssistant согласно основному README
2. **Настройте устройство** через интерфейс HomeAssistant
3. **Проверьте работу** всех функций через интерфейс HomeAssistant

## Поддержка

Если у вас возникли проблемы:

1. Проверьте [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
2. Запустите тестовый скрипт и сохраните вывод
3. Создайте issue с описанием проблемы и логами